<%# Rendered once in the layout so no page has to remember to show its own. %>
<% if flash.any? %>
  <div class="flash">
    <% flash.each do |type, message| %>
      <p class="flash__message flash__message--<%= type %>"><%= message %></p>
    <% end %>
  </div>
<% end %>
